import styles from './Header.module.scss';

const Header = () => {
  return (
    <header className={styles.header}>
      <h1 className={styles.title}>Test Task for Valantis by Damir Kurmanguzhin</h1>
    </header>
  );
};

export default Header;
